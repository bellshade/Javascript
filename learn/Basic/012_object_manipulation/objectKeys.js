// Object.keys()

const person = {
  name: "John Doe",
  age: 24,
  gender: "male"
};

const car = {
  name: "Tesla Model S",
  color: "red"
};

console.log(Object.keys(person)); // [ "name", "age", "gender" ]
console.log(Object.keys(car)); // [ "name", "color" ]
